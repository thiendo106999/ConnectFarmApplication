<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/rlItemVideo"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="20dp"
    android:layout_marginStart="10dp"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/iv_video_thumbnail"
        android:layout_width="@dimen/widthVideo"
        android:layout_height="@dimen/heightVideo"
        android:background="@color/black"
        android:scaleType="centerCrop"
        android:src="@drawable/image" />

    <TextView
        android:id="@+id/tv_video_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/marginStartVideoName"
        android:layout_toEndOf="@+id/iv_video_thumbnail"
        android:maxLines="2"
        android:text="Title"
        android:textColor="@color/colorTextLogin"
        android:textSize="16sp"
        android:textStyle="bold" />



    <TextView
        android:id="@+id/tv_duration"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/tv_video_title"
        android:layout_marginStart="8dp"
        android:layout_marginTop="4dp"
        android:layout_toEndOf="@+id/iv_video_thumbnail"
        android:textColor="@color/black"
        tools:text="00:00:30" />

    <TextView
        android:id="@+id/viewLine"
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:layout_below="@id/iv_video_thumbnail"
        android:layout_marginStart="8dp"
        android:layout_marginTop="20dp"
        android:layout_marginEnd="8dp"
        android:background="@color/my_statusbar_color" />

</RelativeLayout>